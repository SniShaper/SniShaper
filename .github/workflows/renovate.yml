name: renovate

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: renovate/renovate@v39
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
